<div class="quiz-card">
  <div class="flex justify-between items-start mb-4">
    <p class="card-title flex-1"><%= answer.answer_text %></p>
    <%= render "answers/answer_dropdown", answer: answer %>
  </div>
  
  <div class="flex justify-end">
    <% if answer.is_correct? %>
      <span class="difficulty-badge difficulty-easy">✓ Correct</span>
    <% end %>
  </div>
</div>